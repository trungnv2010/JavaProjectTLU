import button from './button'
import calendar from './calendar'
import common from './common'
import ladingCode from './ladingCode'
import unAuth from './unAuth'

export const en = {
  common: { ...common.en },
  unAuth: { ...unAuth.en },
  ladingCode: { ...ladingCode.en },
  button: { ...button.en },
  calendar: { ...calendar.en }
}
export const vn = {
  common: { ...common.vn },
  unAuth: { ...unAuth.vn },
  ladingCode: { ...ladingCode.vn },
  button: { ...button.vn },
  calendar: { ...calendar.vn }
}
